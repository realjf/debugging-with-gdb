## 退出gdb

```
quit [expression]
q
```

要退出GDB，请使用quit命令（缩写为q），或键入文件结尾字符（通常为Ctrl-d）。如果不提供表达式，则GDB将正常终止；否则它将使用表达式的结果作为错误代码终止。

中断（通常为Ctrl-c）不会从GDB退出，而是终止任何正在进行的GDB命令的操作并返回到GDB命令级别。 随时输入中断字符是安全的，因为GDB直到安全时才允许它生效。

如果您一直在使用GDB来控制连接的进程或设备，则可以使用detach命令将其释放。